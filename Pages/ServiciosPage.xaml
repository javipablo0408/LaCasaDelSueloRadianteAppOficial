<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LaCasaDelSueloRadianteApp.ServiciosPage"
             Title=""
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ContentPage.Content>
        <VerticalStackLayout Padding="10" Spacing="10">
            <Label Text="Lista de Servicios"
                   FontSize="22"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{DynamicResource PrimaryTextColor}" />

            <CollectionView x:Name="ServiciosCollectionView"
                            ItemsSource="{Binding ServiciosFiltrados}"
                            SelectionMode="None"
                            Margin="10">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="5"
                               Padding="10"
                               BorderColor="{DynamicResource Primary}"
                               CornerRadius="10"
                               HasShadow="True"
                               BackgroundColor="{DynamicResource SecondaryBackgroundColor}"
                               HorizontalOptions="FillAndExpand"
                               VerticalOptions="FillAndExpand"
                               HeightRequest="80">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavegarADetalleCommand}"
                                    CommandParameter="{Binding}" />
                            </Frame.GestureRecognizers>
                            <Grid HorizontalOptions="FillAndExpand"
                                  VerticalOptions="FillAndExpand">
                                <VerticalStackLayout HorizontalOptions="FillAndExpand"
                                                     VerticalOptions="CenterAndExpand"
                                                     Padding="0">
                                    <Label Text="{Binding TipoServicio}"
                                           FontAttributes="Bold"
                                           FontSize="18"
                                           TextColor="{DynamicResource PrimaryTextColor}" />
                                    <Label Text="{Binding Fecha, StringFormat='{0:dd/MM/yyyy HH:mm}'}"
                                           FontSize="14"
                                           TextColor="{DynamicResource SecondaryTextColor}" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>